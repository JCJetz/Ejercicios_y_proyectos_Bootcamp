{
  "name": "bootcamp",
  "version": "1.0.0",
  "description": "exercises and projects from bootcamp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:weather-app": "npm run deploy --prefix ./proyectos/weather-app",
    "deploy:kanban-app": "npm run deploy --prefix ./proyectos/kanban-board",
    "deploy:rick_and_morty-app": "npm run deploy --prefix ./proyectos/rick_and_morty",
    "deploy:pokedex-app": "npm run deploy --prefix ./proyectos/pokedex-app",
    "deploy:cocktailDB-app": "npm run deploy --prefix ./proyectos/cocktailDB",
    "deploy:calculadora-app": "npm run deploy --prefix ./proyectos/calculadora"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}

