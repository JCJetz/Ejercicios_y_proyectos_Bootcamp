import React from "react";


const Filters = ({ filterAlive, filterDead, filterAll}) => {

    /*
    const filterAlive = () => {
        const aliveFiltered = allcharacters.filter(c => c.status.toLowerCase() === 'alive');
        setCharacters(aliveFiltered) // actualiza el array y lo repinta con el nuevo valor
    };
    const filterDead = () => {
        const deadFiltered = allcharacters.filter(c => c.status.toLowerCase() === 'dead');
        setCharacters(deadFiltered)
    };
      
    const showAll = () => {
        setCharacters(allcharacters);
    };
    
    return (
        
    )
    */


} 

export default Filters;
